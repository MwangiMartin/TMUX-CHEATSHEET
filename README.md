##  TMUX CHEATSHEET

####    Start a new session
```sh
$ tmux new -s <SESSION>
```

####    Start a new session with the name mysession and window mywindow
```sh
$ tmux new -s mysession -n mywindow
```

####    Create window
```sh
$ ctrl + b   c
```